<mptabs
  tabs="{{tabs}}"
  activeTab="{{activeTab}}"
  swiperClass="plus-tabs-swiper"
  bindtabclick="onTabCLick"
  bindchange="onChange"
  activeClass="tab-bar-title__selected"
>
  <view ty:for="{{tabs}}" ty:key="title" slot="tab-content-{{index}}">
    <tyList></tyList>
  </view>
</mptabs>
